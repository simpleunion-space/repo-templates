using Avalonia.Controls;

namespace {{PROJECT_NAME}}.DesktopApp;

/// <summary>
/// Represents the application's initial desktop window.
/// </summary>
public sealed partial class MainWindow : Window
{
    /// <summary>
    /// Initializes the window markup.
    /// </summary>
    public MainWindow()
    {
        InitializeComponent();
    }
}
